# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: app-under-test/e2e/invoices.spec.js >> Invoices >> cancel closes the invoice form
- Location: apps/app-under-test/e2e/invoices.spec.js:30:3

# Error details

```
Error: expect(locator).not.toBeVisible() failed

Locator:  locator('.modal')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for locator('.modal')
    14 × locator resolved to <div class="modal">…</div>
       - unexpected value "visible"

```

```yaml
- text: Before you continue To use InvoicePrepper you must agree to our terms and privacy policy.
- checkbox "I have read and agree to the Terms of Service and Privacy Policy . *"
- text: I have read and agree to the
- button "Terms of Service"
- text: and
- button "Privacy Policy"
- text: . *
- checkbox "I agree to receive email receipts and account notifications." [checked]
- text: I agree to receive email receipts and account notifications.
- button "Continue"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Invoices", () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto("/");
  6  |     await page.locator(".sidebar").waitFor({ timeout: 15000 });
  7  |   });
  8  | 
  9  |   test("can open new invoice form", async ({ page }) => {
  10 |     await page.getByRole("button", { name: /new invoice/i }).click();
  11 |     await expect(page.locator(".modal")).toBeVisible();
  12 |   });
  13 | 
  14 |   test("new invoice form has client name field", async ({ page }) => {
  15 |     await page.getByRole("button", { name: /new invoice/i }).click();
  16 |     await page.locator(".modal").waitFor();
  17 |     await expect(
  18 |       page.locator("input[placeholder*='client' i], input[placeholder*='billed' i], input[placeholder*='customer' i]").first()
  19 |     ).toBeVisible();
  20 |   });
  21 | 
  22 |   test("new invoice form has a line item description field", async ({ page }) => {
  23 |     await page.getByRole("button", { name: /new invoice/i }).click();
  24 |     await page.locator(".modal").waitFor();
  25 |     await expect(
  26 |       page.locator("input[placeholder*='description' i], input[placeholder*='item' i], input[placeholder*='service' i]").first()
  27 |     ).toBeVisible();
  28 |   });
  29 | 
  30 |   test("cancel closes the invoice form", async ({ page }) => {
  31 |     await page.getByRole("button", { name: /new invoice/i }).click();
  32 |     await page.locator(".modal").waitFor();
  33 |     await page.locator(".modal button.btn-ghost").first().click();
> 34 |     await expect(page.locator(".modal")).not.toBeVisible();
     |                                              ^ Error: expect(locator).not.toBeVisible() failed
  35 |   });
  36 | 
  37 |   test("invoice list shows status filters", async ({ page }) => {
  38 |     const filters = page.locator("button, [role='tab']").filter({ hasText: /paid|sent|draft/i });
  39 |     expect(await filters.count()).toBeGreaterThan(0);
  40 |   });
  41 | });
  42 | 
```